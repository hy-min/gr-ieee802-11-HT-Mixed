# Phase 160: 检测自中毒根因修复 + "99.6%"伪影修正 + 残余失败分类

**日期：** 2026-08-05
**状态：** ✅ **CONFIRMED（检测修复）+ ⚠️ 重要数字修正** —— 真实端到端
**69% → 98.9%**；昨日 P159b 的"99.6%/目标达成"是 warmup 分母伪影，
本文件正式修正。

---

## 1. ⚠️ 修正：P159b 的 "99.6%" 是伪影

`usrp_realtime_validate.sh` 的 `est_sent` 只算 3×15s 测量窗（450），但
DECODE_SUCCESS 真值从整个 stderr 计数，**包含 20s warmup 的 ~200 帧**。
45s 轮真实发送 ~650 帧，DS=449 → 真实率 **68.9%**，不是 99.6%。
交叉验证：PDU 计数（msg-queue，不含 warmup）= 311/450 = 69.1%；
300s 轮 PDU=2030/3000 = 67.7%。三种算法一致。**δ 修复本身仍然成立**
（配对 ABAB +221.9、p<1e-4 不受分母影响；链成功率 36%→98% 真实），
但"目标达成"结论作废。**教训：真值计数与 est_sent 分母必须同域。**

## 2. 检测自中毒根因（systematic-debugging，TDD 闭环）

加长验证暴露真实率后，用新鲜 60s IQ 捕获（`p160_gt_extract.py` 找
606 个 GT 帧，L-STF 峰值 638-655 全强）+ file-replay 真实链 DIAG
逐槽对账：

- 漏检 100/606，**全部发生在 SEARCH 态**（无 episode 覆盖），
  漏检帧 L-STF 强度与检出帧完全相同（646.1 vs 646.4）
- 离线复算检测准则：漏检帧处阈值=地板 0.2、平台 ≥1978 样本 ——
  检测条件完全满足却不触发
- **根因**：sync_short SEARCH 分支把整个 chunk 先灌入 4096 自适应窗
  再扫描（前视）。强帧自身 ~2000 样本强相关区超过窗口 10% → p90 跳
  到帧电平（646）→ 阈值 969 > 646 → **帧扼杀对自己的检测**。
  ADAPTIVE_DUMP 实测：374/76586 调用 p90>100（最高 thr=798）。
- **修复**：阈值窗口改**尾随语义**——扫描后只把被扫过的前缀填入窗口
  （`lib/sync_short.cc` SEARCH 分支），帧体永不入窗。
  TDD `p160_poison_unit.py` 4/4（修复前 RED：强帧确定性不检出）。

**验证**：loopback OK=1；回放漏检 100→**2**（99.7%）；USRP 实时
PDU 146-148/150/窗（原 ~104/150）；DS 640/65s（原 ~450）。
提交 `a03c3ad`。

## 3. Margin 重扫（δ-OFF 基线）——2.5 保持最优

4 轮 round-robin（`p160_margin_sweep.sh`）：1.0→389.5 / 1.5→433.0 /
2.0→433.8 / **2.5→453.2**（2.5 vs 1.0 配对 p=0.016）。"2.5 过度门控"
假设 REFUTED；漏检与 margin 无关（真凶是上述自中毒）。默认维持 2.5。

## 4. 当前真实预算（300s 双轮复测）

| 阶段 | 数值 |
|---|---|
| 检测 | ~99.7%（回放实测） |
| 链（检测→好 L-SIG） | ~98.3% |
| FCS（到达→成功） | ~97.6% |
| **端到端** | **98.9%**（PDU 2968/3000） |

## 5. 残余失败分类（99.9% 目标的攻击面）

3000 帧中 ~32 帧损失：
1. **LDPC 终端 21 帧（0.70%）**：时间近 iid；|eq|² 反而偏高（p50
   1.2-1.6，H 偏小→均衡放大）；其中 **~4 帧 is_ht 误判**（ratio_ht
   阈值问题），~17 帧载荷比特错误（待比特级定位：decode_mac 的
   HARD-vs-TX-INTERLEAVED 诊断需要接入 TX 参考）。
2. **检测残余 ~10 帧（0.33%）**：回放残存 2/606 的边缘案例待查。
3. L-SIG 残余 <2 帧。

## 6. 下一步

- decode_mac 接 TX 参考 → 比特级错误定位（尾符号相位漂移？）
- is_ht 误判：ratio_ht 阈值/鲁棒性
- 检测残余：回放 GT#212/#375 上下文分析（零 USRP 时间）

**产物：** `batch_results/p160_margin_sweep/fg_171158/`、
`/home/hy/captures/p160_detect_60s.fc32` + `/tmp/p160_gt_*.npy`、
`/tmp/p160_replay{,2,3}.err`、`/tmp/p160_long{,2,3,4}.*`
**相关：** [[Phase 159b δ REFUTED]]、[[Phase 159a margin]]、
[[Phase 158-ABAB 方法论]]
